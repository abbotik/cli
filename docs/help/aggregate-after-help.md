# Aggregate

Request aggregate calculations for a model.

Common uses:

- `abbot aggregate get users`
- `abbot aggregate run users --count`
- `abbot aggregate run users --sum balance`

Use `--help` on `get` or `run` for the next level down.
