# User Machine Keys

List, create, rotate, and delete tenant machine keys through `/api/user/machine-keys`.

Use this branch after bootstrap when you need to manage uploaded public keys for
tenant-local machine users.

This branch is intentionally concise; use the command itself for the full flag list.
