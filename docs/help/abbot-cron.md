# Cron

Inspect and manage scheduled jobs.

Use this branch for recurring tasks that are stored and controlled by the Abbotik
API.

Common uses:

- `abbot cron list`
- `abbot cron get 123`

Use `--help` on a subcommand for the next level down.
