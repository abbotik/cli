# Aggregate

Request aggregate calculations for a model.

Common uses:

- `abbot api aggregate get users`
- `abbot api aggregate run users --count`
- `abbot api aggregate run users --sum balance`

Use `--help` on `get` or `run` for the next level down.
