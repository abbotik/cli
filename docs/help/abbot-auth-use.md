# Auth Use

Make a saved API host the default for later commands.

```bash
abbot auth use http://localhost:3000
```

The host must already have saved credentials from `abbot auth login <host>`.
Use top-level `--host <host>` when you only need a one-command override.
