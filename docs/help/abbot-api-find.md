# Find

Run filtered and saved queries over records.

Common uses:

- `abbot api find query users --where ...`
- `abbot api find saved users recent`
- `abbot api find query users --select id,name --limit 10`

Use `--help` on `query` or `saved` for the next level down.
