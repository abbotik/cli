# User Introspect

Return the trusted execution context for the current bearer token.

Use this when you need to confirm which tenant, user, auth mode, sudo state, or
key identity the server resolved from your current credentials.

This maps to `/api/user/introspect` in the API docs.
