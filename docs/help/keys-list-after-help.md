# Keys List

List the current user's global bearer API keys.

Use this to inspect durable service tokens minted through `/api/keys`.

For the exact HTTP contract, read `abbot docs path /docs/api/keys`.
