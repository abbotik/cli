# Bulk

Move many records with one payload.

Common uses:

- `abbot api bulk run`
- `abbot api bulk export`
- `abbot api bulk import`
- `abbot api bulk create users`

Use `--help` on a subcommand for the next level down.
