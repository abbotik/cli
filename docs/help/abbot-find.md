# Find

Run filtered and saved queries over records.

Common uses:

- `abbot find query users --where ...`
- `abbot find saved users recent`
- `abbot find query users --select id,name --limit 10`

Use `--help` on `query` or `saved` for the next level down.
