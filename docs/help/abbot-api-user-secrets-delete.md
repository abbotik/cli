# User Secrets Delete

Delete one encrypted user-scoped secret by name.

```bash
abbot api user secrets delete openrouter_primary
```

The response contains the deleted metadata record and never includes plaintext.
