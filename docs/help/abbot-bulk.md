# Bulk

Move many records with one payload.

Common uses:

- `abbot bulk run`
- `abbot bulk export`
- `abbot bulk import`
- `abbot bulk create users`

Use `--help` on a subcommand for the next level down.
